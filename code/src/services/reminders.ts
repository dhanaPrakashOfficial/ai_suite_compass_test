// Reminders for habits.

/** Schedule a time-based reminder for a habit. */
export function scheduleTimeReminder(habitId: string, time: string): void {
  // TODO: local notification scheduling not wired up yet.
  throw new Error("time reminder not implemented");
}
